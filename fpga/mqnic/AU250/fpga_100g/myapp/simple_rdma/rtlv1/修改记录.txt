1208：基于v2版本功能，修改mqnic_app_if_data_proc_v2.v，发送端从tx_config_ram读取源MAC（存放于地址0，低48bit），然后与stream包的第一个512bit中的95-48bit做比对，如果完全吻合，则认为找到了包头，缓存包头，同时将PSN计数清零。
1211：在1208的基础上，修改了寄存器读写部分，增加了寄存器读取类型、版本信息以及通过寄存器写入源MAC，并且可以通过ram端口进行读取（地址0，mac信息存于512bit数据中的低48bit）；
1214：在1211的基础上，增加了接收端部分信息（包头、包头对应的Keep、dest_QPN、版本号）的锁存，可通过APP的AXI-LITE寄存器进行读取。